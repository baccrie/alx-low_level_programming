#include "main.h"
/**
 * _isupper - evalue if letter is uppercase
 * @c: is an integer
 * Return: Always 0.
 */

int _isupper(int c)
{
if (c <= 'Z' && c >= 'A')
{
return (1);
}
else
{
return (0);
}
}
