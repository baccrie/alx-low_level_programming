#include <stdio.h>
/**
 * main - Entry point
 * Task 3. alphABET
 * Return: Always 0 (Success)
 */
int main(void)
{
int numb;
numb = 0;
while (numb < 10)
{
printf("%d", numb);
++numb;
}
putchar('\n');
return (0);
}
